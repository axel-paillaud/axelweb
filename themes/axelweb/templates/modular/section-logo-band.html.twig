<section class="section-logo-band">
    <div class="marquee">
        <div class="marquee-track">
            {% for logo in page.header.logos %}
                {% set image = page.media[logo.file] %}
                {% if image %}
                    <div class="marquee-item">
                        <a href="{{ logo.url }}" target="_blank" rel="noopener noreferrer nofollow">
                            <img src="{{ image.url }}" alt="{{ logo.alt }}" loading="lazy">
                        </a>
                    </div>
                {% endif %}
            {% endfor %}
            {# Duplicate for seamless loop #}
            {% for logo in page.header.logos %}
                {% set image = page.media[logo.file] %}
                {% if image %}
                    <div class="marquee-item" aria-hidden="true">
                        <a href="{{ logo.url }}" target="_blank" rel="noopener noreferrer nofollow" tabindex="-1">
                            <img src="{{ image.url }}" alt="" loading="lazy">
                        </a>
                    </div>
                {% endif %}
            {% endfor %}
        </div>
    </div>
</section>
